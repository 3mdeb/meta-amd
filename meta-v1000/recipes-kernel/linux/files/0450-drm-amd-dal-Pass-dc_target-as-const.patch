From 2019f2fd11dc60dbfc753432d9ec0e14225ff0f7 Mon Sep 17 00:00:00 2001
From: Dmytro Laktyushkin <dmytro.laktyushkin@amd.com>
Date: Mon, 1 Feb 2016 16:13:26 -0500
Subject: [PATCH 0450/1722] drm/amd/dal: Pass dc_target as const

Signed-off-by: Dmytro Laktyushkin <dmytro.laktyushkin@amd.com>
Acked-by: Harry Wentland <harry.wentland@amd.com>
Signed-off-by: Alex Deucher <alexander.deucher@amd.com>
Signed-off-by: Kalyan Alle <kalyan.alle@amd.com>
---
 drivers/gpu/drm/amd/dal/dc/core/dc_target.c | 4 ++--
 drivers/gpu/drm/amd/dal/dc/dc.h             | 4 ++--
 2 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/drivers/gpu/drm/amd/dal/dc/core/dc_target.c b/drivers/gpu/drm/amd/dal/dc/core/dc_target.c
index 4b5504c..4fd42a5 100644
--- a/drivers/gpu/drm/amd/dal/dc/core/dc_target.c
+++ b/drivers/gpu/drm/amd/dal/dc/core/dc_target.c
@@ -67,14 +67,14 @@ static void destruct(struct core_target *core_target)
 	}
 }
 
-void dc_target_retain(struct dc_target *dc_target)
+void dc_target_retain(const struct dc_target *dc_target)
 {
 	struct target *target = DC_TARGET_TO_TARGET(dc_target);
 
 	target->ref_count++;
 }
 
-void dc_target_release(struct dc_target *dc_target)
+void dc_target_release(const struct dc_target *dc_target)
 {
 	struct target *target = DC_TARGET_TO_TARGET(dc_target);
 	struct core_target *protected = DC_TARGET_TO_CORE(dc_target);
diff --git a/drivers/gpu/drm/amd/dal/dc/dc.h b/drivers/gpu/drm/amd/dal/dc/dc.h
index b8d0786..8d6fc6b 100644
--- a/drivers/gpu/drm/amd/dal/dc/dc.h
+++ b/drivers/gpu/drm/amd/dal/dc/dc.h
@@ -235,8 +235,8 @@ struct dc_target *dc_create_target_for_streams(
 const struct dc_target_status *dc_target_get_status(
 					const struct dc_target* dc_target);
 
-void dc_target_retain(struct dc_target *dc_target);
-void dc_target_release(struct dc_target *dc_target);
+void dc_target_retain(const struct dc_target *dc_target);
+void dc_target_release(const struct dc_target *dc_target);
 void dc_target_log(
 	const struct dc_target *dc_target,
 	struct dal_logger *dal_logger,
-- 
2.7.4

