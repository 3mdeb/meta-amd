From 92a5ce4966b1de81381ec4f25f1b0dd9cae2e170 Mon Sep 17 00:00:00 2001
From: Harry Wentland <harry.wentland@amd.com>
Date: Wed, 15 Jul 2015 08:51:08 -0400
Subject: [PATCH 364/401] amd/dal: Fix compile warnings

Signed-off-by: Harry Wentland <harry.wentland@amd.com>
Reviewed-by: Mykola Lysenko <mykola.lysenko@amd.com>
Signed-off-by: Sanjay R Mehta <sanju.mehta@amd.com>
---
 drivers/gpu/drm/amd/dal/audio/dce110/hw_ctx_audio_dce110.c        | 1 -
 drivers/gpu/drm/amd/dal/hw_sequencer/dce110/hw_sequencer_dce110.c | 2 --
 drivers/gpu/drm/amd/dal/hw_sequencer/hw_sequencer.c               | 3 ++-
 drivers/gpu/drm/amd/dal/topology/tm_resource_builder.c            | 1 -
 4 files changed, 2 insertions(+), 5 deletions(-)

diff --git a/drivers/gpu/drm/amd/dal/audio/dce110/hw_ctx_audio_dce110.c b/drivers/gpu/drm/amd/dal/audio/dce110/hw_ctx_audio_dce110.c
index 80a35112..2cbd3fd 100644
--- a/drivers/gpu/drm/amd/dal/audio/dce110/hw_ctx_audio_dce110.c
+++ b/drivers/gpu/drm/amd/dal/audio/dce110/hw_ctx_audio_dce110.c
@@ -1029,7 +1029,6 @@ static void configure_azalia(
 	const struct audio_info *audio_info)
 {
 	uint32_t speakers = audio_info->flags.info.ALLSPEAKERS;
-	bool audio_interface = audio_info->flags.info.SUPPORT_AI;
 	uint32_t value;
 	uint32_t field = 0;
 	enum audio_format_code audio_format_code;
diff --git a/drivers/gpu/drm/amd/dal/hw_sequencer/dce110/hw_sequencer_dce110.c b/drivers/gpu/drm/amd/dal/hw_sequencer/dce110/hw_sequencer_dce110.c
index 4fa0bd1..d2f88bd 100644
--- a/drivers/gpu/drm/amd/dal/hw_sequencer/dce110/hw_sequencer_dce110.c
+++ b/drivers/gpu/drm/amd/dal/hw_sequencer/dce110/hw_sequencer_dce110.c
@@ -500,8 +500,6 @@ static void set_display_clock(
 	const struct minimum_clocks_calculation_result *min_clocks)
 {
 	struct hw_global_objects objs = { NULL };
-	uint32_t i;
-	uint32_t paths_number;
 
 	dal_hw_sequencer_get_global_objects(path_set, &objs);
 
diff --git a/drivers/gpu/drm/amd/dal/hw_sequencer/hw_sequencer.c b/drivers/gpu/drm/amd/dal/hw_sequencer/hw_sequencer.c
index f495f65..29e15bc 100644
--- a/drivers/gpu/drm/amd/dal/hw_sequencer/hw_sequencer.c
+++ b/drivers/gpu/drm/amd/dal/hw_sequencer/hw_sequencer.c
@@ -1613,10 +1613,11 @@ enum hwss_result dal_hw_sequencer_set_backlight_adjustment(
 	struct display_path *display_path,
 	struct hw_adjustment_value *adjustment)
 {
+	struct display_path_objects obj;
+
 	if (adjustment == NULL)
 		return HWSS_RESULT_ERROR;
 
-	struct display_path_objects obj;
 	dal_hw_sequencer_get_objects(display_path, &obj);
 
 	if (dal_display_path_get_controller(display_path) == NULL
diff --git a/drivers/gpu/drm/amd/dal/topology/tm_resource_builder.c b/drivers/gpu/drm/amd/dal/topology/tm_resource_builder.c
index 55e8f64..a02b8d8 100644
--- a/drivers/gpu/drm/amd/dal/topology/tm_resource_builder.c
+++ b/drivers/gpu/drm/amd/dal/topology/tm_resource_builder.c
@@ -313,7 +313,6 @@ enum tm_result tm_resource_builder_create_gpu_resources(
 {
 	enum tm_result rc = TM_RESULT_SUCCESS;
 	struct gpu *gpu;
-	struct dal_context *dal_context = tm_rb->dal_context;
 
 	do {
 		gpu = tm_resource_builder_create_gpu(tm_rb);
-- 
1.9.1

