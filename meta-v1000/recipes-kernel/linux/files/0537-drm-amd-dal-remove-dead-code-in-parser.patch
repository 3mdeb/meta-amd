From 9e64016735c0fc1e40b8e45e7ab6df20ac29f13e Mon Sep 17 00:00:00 2001
From: Dave Airlie <airlied@redhat.com>
Date: Tue, 12 Apr 2016 06:28:19 +1000
Subject: [PATCH 0537/1722] drm/amd/dal: remove dead code in parser.

These functions don't do anything, and are never called.

Signed-off-by: Dave Airlie <airlied@redhat.com>
Acked-by: Harry Wentland <harry.wentland@amd.com>
Signed-off-by: Alex Deucher <alexander.deucher@amd.com>
Signed-off-by: Kalyan Alle <kalyan.alle@amd.com>
---
 drivers/gpu/drm/amd/dal/dc/bios/bios_parser.c | 73 ---------------------------
 1 file changed, 73 deletions(-)

diff --git a/drivers/gpu/drm/amd/dal/dc/bios/bios_parser.c b/drivers/gpu/drm/amd/dal/dc/bios/bios_parser.c
index cecb30e..cca9f58 100644
--- a/drivers/gpu/drm/amd/dal/dc/bios/bios_parser.c
+++ b/drivers/gpu/drm/amd/dal/dc/bios/bios_parser.c
@@ -4602,79 +4602,6 @@ bool dc_bios_is_lid_open(
 	BREAK_TO_DEBUGGER();
 	return false;
 }
-#if 0
-
-static bool is_lid_status_changed(
-	struct dc_bios *bios)
-{
-	BREAK_TO_DEBUGGER();
-	return false;
-}
-
-static bool is_display_config_changed(
-	struct dc_bios *bios)
-{
-	BREAK_TO_DEBUGGER();
-	return false;
-}
-
-static enum lcd_scale get_scratch_lcd_scale(
-	struct dc_bios *bios)
-{
-	BREAK_TO_DEBUGGER();
-	return LCD_SCALE_NONE;
-}
-
-void dc_bios_get_bios_event_info(
-	struct dc_bios *bios,
-	struct bios_event_info *info)
-{
-	BREAK_TO_DEBUGGER();
-}
-
-void dc_bios_update_requested_backlight_level(
-	struct dc_bios *bios,
-	uint32_t backlight_8bit)
-{
-	BREAK_TO_DEBUGGER();
-}
-
-uint32_t dc_bios_get_requested_backlight_level(
-	struct dc_bios *bios)
-{
-	BREAK_TO_DEBUGGER();
-	return 0;
-}
-
-void dc_bios_take_backlight_control(
-	struct dc_bios *bios,
-	bool cntl)
-{
-	BREAK_TO_DEBUGGER();
-}
-
-bool dc_bios_is_active_display(struct dc_bios *bios,
-			       enum signal_type signal,
-			       const struct connector_device_tag_info *device_tag)
-{
-	BREAK_TO_DEBUGGER();
-	return false;
-}
-
-static enum controller_id get_embedded_display_controller_id(
-	struct dc_bios *bios)
-{
-	BREAK_TO_DEBUGGER();
-	return CONTROLLER_ID_UNDEFINED;
-}
-
-static uint32_t get_embedded_display_refresh_rate(
-	struct dc_bios *bios)
-{
-	BREAK_TO_DEBUGGER();
-	return 0;
-}
-#endif
 
 static bool bios_parser_construct(
 	struct bios_parser *bp,
-- 
2.7.4

